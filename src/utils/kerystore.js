const AppPath = "BOOTCAMP.SILLABUZ"

const keystore = {
    user_name: AppPath + "_USER_NAME"
}

// localStorage.setItem(keystore.user_name, 'shair')
// localStorage.getItem(keystore.user_name)

export default keystore